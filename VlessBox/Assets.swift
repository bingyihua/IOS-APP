import SwiftUI

// MARK: - Color Extensions
extension Color {
    static let systemBackground = Color(.systemBackground)
    static let systemGroupedBackground = Color(.systemGroupedBackground)
    static let systemGray6 = Color(.systemGray6)
}

// MARK: - App Icon (placeholder)
/*
 To add app icon:
 1. Create 1024x1024 PNG
 2. Add to Resources/Images.xcassets/AppIcon.appiconset/
 3. Reference in Info.plist as CFBundleIconFile
 */

// MARK: - Assets placeholder
/*
 Info.plist references:
 - CFBundleIconName: AppIcon
 */
